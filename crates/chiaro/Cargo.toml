[package]
name = "chiaro"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reusable, UI-independent Light L16 LRI support"

[dependencies]
chiaro-proto.workspace = true
memmap2.workspace = true
thiserror.workspace = true
zune-jpeg.workspace = true

[features]
# Synthetic LRI generation for tests, examples, and UI development.
mock = []

[dev-dependencies]
tempfile.workspace = true

[[example]]
name = "make_mock_lri"
required-features = ["mock"]
